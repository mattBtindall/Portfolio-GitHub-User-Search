API keys are hidden in a config file (module) that are imported at the top of main.js. 

In order for this application to run, create a file and a export an object that contains a 'oAuthApiToken' property with it's value being your GitHub OAuth token. 